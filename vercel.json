{
  "projects": [
    {
      "src": "client",
      "use": "@vercel/static-build",
      "buildCommand": "npm run build",
      "outputDirectory": "dist"
    },
    {
      "src": "server",
      "use": "@vercel/node",
      "outputDirectory": "api"
    }
  ]
}
